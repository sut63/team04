// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/B6001186/Contagions/ent/severity"
	"github.com/facebookincubator/ent/dialect/sql"
)

// Severity is the model entity for the Severity schema.
type Severity struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SeverityName holds the value of the "SeverityName" field.
	SeverityName string `json:"SeverityName,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SeverityQuery when eager-loading is set.
	Edges SeverityEdges `json:"edges"`
}

// SeverityEdges holds the relations/edges for other nodes in the graph.
type SeverityEdges struct {
	// Disease holds the value of the disease edge.
	Disease []*Disease
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DiseaseOrErr returns the Disease value or an error if the edge
// was not loaded in eager-loading.
func (e SeverityEdges) DiseaseOrErr() ([]*Disease, error) {
	if e.loadedTypes[0] {
		return e.Disease, nil
	}
	return nil, &NotLoadedError{edge: "disease"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Severity) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // SeverityName
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Severity fields.
func (s *Severity) assignValues(values ...interface{}) error {
	if m, n := len(values), len(severity.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	s.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field SeverityName", values[0])
	} else if value.Valid {
		s.SeverityName = value.String
	}
	return nil
}

// QueryDisease queries the disease edge of the Severity.
func (s *Severity) QueryDisease() *DiseaseQuery {
	return (&SeverityClient{config: s.config}).QueryDisease(s)
}

// Update returns a builder for updating this Severity.
// Note that, you need to call Severity.Unwrap() before calling this method, if this Severity
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Severity) Update() *SeverityUpdateOne {
	return (&SeverityClient{config: s.config}).UpdateOne(s)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (s *Severity) Unwrap() *Severity {
	tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Severity is not a transactional entity")
	}
	s.config.driver = tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Severity) String() string {
	var builder strings.Builder
	builder.WriteString("Severity(")
	builder.WriteString(fmt.Sprintf("id=%v", s.ID))
	builder.WriteString(", SeverityName=")
	builder.WriteString(s.SeverityName)
	builder.WriteByte(')')
	return builder.String()
}

// Severities is a parsable slice of Severity.
type Severities []*Severity

func (s Severities) config(cfg config) {
	for _i := range s {
		s[_i].config = cfg
	}
}
